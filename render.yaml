services:
  - type: web
    name: bilbo-web
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    region: oregon
    envVars:
      - key: BILBO_DB_PATH
        value: /tmp/bilbo.db
      - key: PORT
        value: 8000
    healthCheckPath: /health
